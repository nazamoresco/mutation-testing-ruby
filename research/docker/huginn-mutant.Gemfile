# Ephemeral Bundler overlay for the historical Huginn mutation study.
# The application Gemfile and lockfile remain unmodified in the source checkout.
eval_gemfile "/app/Gemfile"

gem "mutant-rspec", "0.14.2"
