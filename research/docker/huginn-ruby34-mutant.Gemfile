# Ephemeral Bundler overlay for the Ruby 3.4 historical-control probe.
# The Huginn Gemfile and lockfile remain unmodified in the archived snapshot.
eval_gemfile "/app/Gemfile"

gem "mutant-rspec", "0.17.0"
