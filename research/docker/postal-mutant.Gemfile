# Ephemeral Bundler overlay for the Postal historical pair.
# It preserves the archived application Gemfile and lockfile, adding only the
# same pinned Mutant integration to each container image.
eval_gemfile "/opt/postal/app/Gemfile"

gem "mutant-rspec", "0.17.0"
