# dev.to — baseline histórico del padre del fix

Fecha: 2026-09-23  
Aplicación: `dev.to` / Forem  
Snapshot: `3cd0a548a63bff7e9712876e08573fdbaec7ae66`  
Rol: padre del fix `9db33e70` (`DetailsTag#render`)  
Estado: **baseline_failed**

## Celda ejecutada

Se construyó una imagen derivada de Ruby 3.3.0 y Rails 8.0.5 usando el
`Gemfile.lock` del snapshot del fix para normalizar la única diferencia Ruby
con el control previsto (`pry` / `pry-rails`). La imagen instaló 413 gemas,
Yarn 4.1.0 y los assets de test sin errores fatales. La receta y la imagen
derivada no modificaron los worktrees de Forem.

La corrida aislada usó PostgreSQL 13 con pgvector y Redis 7 en una red Docker
propia, y ejecutó:

```sh
bundle exec rails db:create
bundle exec rails db:schema:load
bundle exec rspec
```

## Resultado

La suite serial completa empezó correctamente, cargó el esquema y produjo
progreso RSpec durante al menos 47 minutos con uso sostenido de CPU. La salida
observada contiene múltiples marcadores `F` —incluido un bloque grande de
fallos—, por lo que el baseline no es verde y el snapshot no cumple la puerta
para Mutant ni para el contraste histórico.

El contenedor se ejecutó con `--rm` y la sesión de terminal expiró después de
su finalización. Por ello no se conserva un conteo final fiable de ejemplos,
fallos o pendientes; **no se infieren ni se inventan esas cifras**. El estado
`baseline_failed` se apoya únicamente en los marcadores de fallo observados.

## Decisión

No se ejecuta Mutant para el fix ni se construye/ejecuta el baseline del
control `0fdfd1eb`. Repetir sólo tiene sentido si se prepara una ejecución que
persista el formatter y el resultado de RSpec; no puede convertir esta corrida
fallida en evidencia causal.
