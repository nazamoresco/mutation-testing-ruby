# Baseline — Klaxon

Fecha: 2026-09-12  
Resultado: aprobado; dos corridas verdes en copias independientes

## Fuente y entorno

- Repositorio: `https://github.com/themarshallproject/klaxon`
- SHA: `ad2cb6ca1219b94ee7b2a032308f72a3997469fc`
- Runner temporal: `ruby:3.4.8-slim`, Linux arm64; Rails 8.1.3.1 y RSpec.
- PostgreSQL 16 aislado con el usuario y la base definidos por el workflow CI.
- Cada pasada usó una copia temporal sin `.git`; gems en volumen temporal. Se
  instalaron Node.js y Git sólo dentro del contenedor de ejecución.

## Ejecución

- Flujo equivalente a CI: `assets:precompile`, `assets:clean`, preparación del
  esquema PostgreSQL y `rspec --tag ~skip --tag ~pending`.
- Corrida 1: código 0; contenedor 72,06 s.
- Corrida 2: código 0; contenedor 80,85 s; copia nueva y base recreada.
- No se observaron fallas ni flakiness. Las advertencias deprecadas de Sass no
  afectaron el código de salida.

## Decisión

Klaxon cumple el criterio de baseline de la cohorte. La integración de Mutant
se hará únicamente en un sandbox temporal y empezará con un selector pequeño;
no se modificará ni publicará código del repositorio de origen.
