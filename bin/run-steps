#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "$0")/.." && pwd)"
for step in "$root"/steps/[0-9][0-9]_*.rb; do
  echo "==> $(basename "$step")"
  bundle exec ruby -I"$root/lib" "$step"
done
